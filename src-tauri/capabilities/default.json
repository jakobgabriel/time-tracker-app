{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Tempo talks to the network from Rust only, so the webview needs the core IPC surface plus permission to hand an obsidian:// link to the system.",
  "windows": [
    "main"
  ],
  "platforms": [
    "android",
    "iOS",
    "linux",
    "macOS",
    "windows"
  ],
  "permissions": [
    "core:default",
    "opener:allow-open-url"
  ]
}
